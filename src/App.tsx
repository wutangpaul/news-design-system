export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-8">
      <h1 className="font-serif text-h2 font-bold">News Design System</h1>
      <p className="text-text-secondary">
        Component catalogue lives in Storybook — run{" "}
        <code className="font-mono text-small">yarn storybook</code>.
      </p>
    </main>
  );
}
